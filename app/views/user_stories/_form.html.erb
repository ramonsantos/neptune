<%= form_with(model: user_story, local: true) do |form| %>
  <% if user_story.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_story.errors.count, "error") %> prohibited this user_story from being saved:</h2>

      <ul>
        <% user_story.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :release_id %>
    <%= form.text_field :release_id %>
  </div>

  <div class="field">
    <%= form.label :situation %>
    <%= form.select :situation, UserStory.situations.values, selected: UserStory.situations[@user_story.situation] %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
